Uses D3 and a JSON file, pdus.json. A relatively simple way to construct
a description of DIS that shows the PDUs and their family/group
membership.

The files should be placed on an actual web server, which can be local.
